The compiled BusyBox executable is not committed to Git.

It is produced by:

    scripts/build-busybox

and distributed as a TreeForge provider release asset.

The corresponding BusyBox source archive and exact build configuration are
retained with the provider metadata for source/rebuild compliance.
