# Payload policy

The compiled `kexec` payload is generated by `scripts/build-kexec-tools`
and is not stored directly in the source repository.

Published provider releases contain the compiled payload together with
its source, license, integrity, and build-provenance records.
